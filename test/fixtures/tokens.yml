# Read about fixtures at http://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

one:
  access_token: <%= 128.times.map {|i| rand(1..9)}.join %>
  user_id: Alex@test.com
  expiration_date: <%= 5.days.from_now.to_datetime %>

two:
  access_token: <%= 128.times.map {|i| rand(1..9)}.join %>
  user_id: Bob@test.com
  expiration_date: <%= 10.minutes.from_now.to_datetime %>
